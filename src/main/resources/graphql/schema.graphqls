type Query {
    boardList: [Board]
    board(id: ID): Board
}

type Board {
    id: ID
    title: String
    content: String
}

type Mutation {
    create(title: String, content: String): Board
}

input BoardInput {
    title: String
    content: String
}
